#!/bin/bash

# Script to test vizabi during continuous integration (sourced)

# The following env vars needs to be set properly:
# - DRONE_BUILD_DIR
# - COVERAGE

# The current working directory should be the vizabi project root folder

echo TODO